name: gitleaks

on:
  push:
    branches: [main] # PR branches are scanned once, via the pull_request event
  pull_request:
  workflow_dispatch:
  schedule:
    - cron: '0 4 * * 1' # weekly full-history sweep, Mondays 04:00 UTC

permissions:
  contents: read
  pull-requests: read # gitleaks-action enumerates the PR's commits on pull_request events

jobs:
  scan:
    name: gitleaks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0 # full history so the scan covers every commit
      # Third-party action, SHA-pinned; Dependabot updates the pin monthly.
      - uses: gitleaks/gitleaks-action@e0c47f4f8be36e29cdc102c57e68cb5cbf0e8d1e # v3.0.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # GITLEAKS_LICENSE is only required for organization-owned repos, not personal ones.
